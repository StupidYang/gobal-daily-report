# 2026-09-23 原生任务交接修复

## 已复现的故障

- global-main / 20260923T145853-global-main：模型使用 name 表达框架名，校验器只读 framework，完整六资产内容被整体拒绝。
- asia-session / 20260923T153900-asia-session：区域组装覆盖本轮已采集报价，并重新引入18条缺语义字段的历史新闻，导致预检失败。
- 预检失败后执行锁仍为 analyzing，且原提交不可修改，没有有界修订通道。
- 展开新闻分页后的390px页面被日度收益率长文本撑宽。

## 修复

- 在语义校验前统一框架字段别名，不补造缺失结论。
- 区域任务原子提交 quotes、区域股票、news、synthesis；其他依赖继续使用原冻结快照。
- 合格旧新闻保留原时点；不完整历史新闻独立显示并注明待复核，不计入本轮合格新闻。新新闻缺字段仍拒绝。
- 首次正文校验失败返回 needs-revision，可在原20分钟截止前仅提交 --r1 一次；第二次失败终结执行，不续期。
- 从精确事件提交SHA读取交接文件，保留令牌、代数、来源包哈希及候选哈希校验。
- 提供执行阶段公开状态，区分采集、待分析、待修订、失败、仓库提交和实际发布。
- 日度收益率文字允许换行，不隐藏日期；浏览器验收实际展开所有新闻分页。

## 已执行验证

测试提交 51865432acc0c23cb7d74b9175c00e98ccdba530；GitHub运行35838497183成功。

- 223项代码测试通过，0失败、0跳过。
- 两份原始失败提交和对应来源包按固定Git SHA保留，未改原始正文以迎合测试。
- 主任务回放7模块、6资产详报、5框架、14条本轮新闻、18条待复核历史新闻。
- 区域任务回放4模块、6资产详报、4框架、18条待复核历史新闻，保留本轮行情。
- 两份回放都通过390/1440宽度的真实HTTP浏览器交互与全文检查。
- 回放是历史故障复现，不是本时刻新行情，也不是无人干预定时任务运行成功。

三个原生调度保持暂停，不新增定时任务。真实请求与生产发布须单独核验回执及公网版本；不以本文件或测试绿灯代替该证据。
