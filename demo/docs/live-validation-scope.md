# 2026-09-23 真实采集与执行修复验收边界

本次固定真实来源包：2026-09-23T02:54:29.233Z至02:54:35.008Z，实测5775ms，72条返回、0项请求错误。31项必看包括30项带真实来源时点的数值与1项财政部日度两年收益率，另有41家美股配置候选报价。来源成功不等于所有观测都是实时；日经等较旧数据明确保留原日期。

固定新分析于2026-09-23T02:59:54.642Z完成并绑定来源包SHA256 1a10db3ae9fb4b3385a198084522fe53b7e826ea770b16f827783b4e0ade80a4。隔离报告2026-09-23-1120涵盖六类资产完整分析、机制、风险、改判条件、时间尺度及来源。CI再次组装327ms，没有改写分析结束时间或伪造本轮11:20报价。原生定时任务从触发到生成全文的耗时未由这两个数字代表。

运行35813935434中，210项原有与新增测试、真实GitHub两方旧SHA写冲突、390/1440 HTTP浏览器及展开内容均通过。最后自动推送被GitHub工作流写权限拦截；测试产物与提交对象205b89be515a098b446b0af01abd87150bcbad99保留，由有授权的仓库连接继续交付，不修改CI凭据权限。

补充3项事件路由测试：不依赖Actions中缺失的commit.added字段，按事件固定提交SHA读取文件列表，拒绝修改既有请求及多请求混写。最后总数213项；实际部署状态以随后发布工作流及公网哈希检查为准。

真实CAS检查保存于docs/live-lock-proof.json：两个同时使用同一旧SHA的写请求，1成功、1冲突；三个任务组都不能进入已有执行，失败者令牌失效。用的是独立validation锁，没有修改生产锁。

采集单请求8秒、整体90秒、并发4。采集/分析输出许可固定20分钟，过期后旧令牌拒绝提交；原生ChatGPT界面不能被此代码强制终止。publishing状态不靠期限抢占，须先查真实工作流已结束并核验main回执再释放。Actions进程另受5分钟及子进程240秒限制。

尚未覆盖：原生任务在无人干预下完整生成新分析并发布的性能、连续多轮运行稳定性；5份已读取材料不构成全量新闻覆盖，正文近期新闻3条；行业全量成员与热度量价输入、ETF/杠杆资金、全公司财报研究仍不足。禁止把这些覆盖不足写成已完成，也不得强凑新闻或排名。

/validation/是固定真实数据验收页面，/demo/仍是合成数据演示。main的原生产报告和历史没有被本次试跑替换。三个原生任务仍暂停，数量和频率不变；恢复需要用户明确授权，不因CI通过自动重启。
