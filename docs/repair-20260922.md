# 2026-09-22 隔离修复与调度审计

## 明确边界

生产开关保持暂停。人工合成数据只发布在 `/demo/`，正式 `data/latest.json`、模块、不可变run及历史报告不因修复而改变。工程验收不能作为真实报价、新闻来源或原生定时执行成功的证明。既有15:58正式报告保留并显示维护标识。

## 修复清单

- 缺依赖状态不再因5次无关检查变为永久拒绝；依赖到齐可以重新校验。
- 时间引用使用综合报告的真实生成秒级时间；不把同一分钟内的模块误判为未来数据。
- 新综合报告必须冻结全部六个生产模块，不允许空引用冒充完整发布。
- 三种任务都必须包含完整synthesis。整批候选先在临时目录校验，成功后模块、报告、历史、索引与回执原子写入；失败回滚。
- 暂停开关在实际发布代码中生效；测试输入在未标记生产目录被拒绝，测试页面有常驻醒目标识和独立偏好存储。
- 兼容旧报告详报名字段与观察/定义字段，避免空标题；测试时钟明确为合成时点。
- 自定义工作流收敛至只读CI、唯一发布器、手动批次审计。已退休YAML存到docs/retired-workflows，以.txt结尾不会执行。GitHub系统Pages构建不算自建业务工作流。
- Pages已有部署或已经提供相同版本时不重复请求部署；检查预期字节而不只看绿色图标。

## 原生任务数量为何膨胀

审计起点有16条历史任务记录：7个常驻角色任务，以及9个初始化/验收一次性记录。它们不等于16个正在运行的任务；审计时全部停用。7个常驻定义把内部模块职责与外部触发器混在一起，初始化与验收又制造了副本。

## 收敛后的三个入口（仍停用）

| 任务 | 时间 | 内部职责 | 复用的原任务ID |
|---|---|---|---|
| global-main | UTC+8 00/04/08/12/16/20 | 六生产模块与完整综合报告 | 6ab1dc6f6d6081919ac20591b5ee4988 |
| asia-session | 周一至五 UTC+8 09:35/15:35/16:35 | A股港股与综合报告 | 6aafc701dc80819187ea120074ccef44 |
| us-session | 周一至五 纽约09:35/16:35 | 美股与综合报告 | 6ab1cb9983d88191bf0db6340bfe6972 |

交易节点先核对真实交易日历，节假日/半日市不按普通交易日强凑数据。七个模块保留内容职责，不再有七次独立原生调度。其余4个原常驻任务继续退役停用，9个一次性任务不重启、不复制；历史记录不是已被物理删除。

原定义每天37次常驻启动；新定义工作日最多11次、周末6次，交易假日还应跳过无意义的区域发布。该数字只代表调度次数，不是token消耗承诺；每次主报告仍包含原有信息职责。

## 验收与恢复门槛

独立合成数据跑真实发布器和浏览器，验证历史与索引一致、故障恢复、缺失不补零、禁止污染、原子回滚、四种视口、完整正文与来源导航。演示包含7份四小时快照、31个必看资产（含有意缺失项）、43个行业样本、41个美股候选和18条合成事件。

恢复必须同时满足：代码和界面回归通过，实际公网文件身份与预期一致，三个实际平台任务与仓库配置一致，真实来源只读试跑验证且缺口如实标注。只有合成测试成功时不得恢复。待源验证完成后再打开生产开关与三个现有任务；不会因为某次CI绿灯自动开启。
