# 2026-09-22 发布事故修复

## 实锤问题
- 部分asOf只有日期，quotes/macro被严格校验拒绝；前端将一条异常放大成整个模块不可见。
- synthesis只写发布计划而无payload.report，首页一直停在09:35。
- 77项夹具测试通过但真实模块校验失败；Pages的旧部署独立于检查。

## 修复边界
- 写入仍严格，读取只隔离异常报价，不补零、不猜时刻、不修改原始run。
- 空行业目录折叠，真实marketSummary、observedThemes和已核验候选样本先展示；缺数据不声称已形成Top-N。
- 正式发布改为代码消费data/inbox候选，生成data/receipts回执；不再把提示词当事务。
- 合成模块入库前必须通过完整报告验证，引用的不可变模块必须存在且不晚于报告。
- 首页与模块时点分别显示；刷新不代表新研究完成。

## 运行
`node scripts/promote-candidates.cjs` 消费候选；`node scripts/check-published.cjs` 检查真实正式数据与历史。已有坏run保留作为事故证据，修订用新runId。

## 未夸大完成
结构校验不证明数字真实性；免费公开来源不保证全行业量比/换手基准；榜单样本与完整行业必须区分。15:00首次出版不应冒充已经核验了15:00之后才公布的完整收盘资料。
